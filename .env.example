# LinkedIn OAuth credentials
LINKEDIN_CLIENT_ID=your_linkedin_client_id
LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret
# Optional: change this if you run the helper on a different host or port
LINKEDIN_REDIRECT_URI=http://localhost:5000/callback

# Optional: Flask session secret for state validation
FLASK_SECRET_KEY=replace_with_a_random_secret
